  <!-- Canonical Tag -->
  {%- assign canonical_url = shop.url | append: collection.url -%}
  {%- if paginate.page > 1 -%}
    <link rel="canonical" href="{{ canonical_url }}" />
  {%- else -%}
    <link rel="canonical" href="{{ canonical_url }}" />
  {%- endif -%}
